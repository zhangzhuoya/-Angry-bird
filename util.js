function createEle(eleName,classArr,styleObj){
    var dom = document.createElement(eleName);
    //for (let index = 0; classArr.length; index++) {
    //    dom.classList.add(classArr[i])       
   // }
   // for (var i in styleObj) {
    //  dom.style[i] = styleObj[i] 
    //}
    return dom
}